
// Display a welcome message in the console
console.log("Welcome to the Workstation Scheduler!");

// Add interactivity for buttons or other elements
document.addEventListener("DOMContentLoaded", () => {
    // Example: Adding functionality to a navigation button
    const schedulerButton = document.querySelector(".button");
    schedulerButton.addEventListener("click", () => {
        console.log("Navigating to the Scheduler...");
    });
});
